const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: 'subagent_admin@schoolpro.com' }, include: { tenantSuperAdmin: true } }); if (!user) return console.log('User not found'); const token = Buffer.from(user.id + ':' + user.tenantSuperAdmin.tenantId).toString('base64'); console.log('Link: https://school-pro-mocha-beta.vercel.app/auth/verify?token=' + token); } main().catch(console.error).finally(() => prisma.$disconnect());
